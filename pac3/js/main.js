import {getRandomPkmn } from "./pokemon.js";



/*const url = "https://pokeapi.co/api/v2/pokemon/";
const id = 6;

fetch(url + id)
.then(x => x.json())
.then(y => {
    console.dir(y);
    console.log(y.id);
    console.log(y.name);
    console.log(y.height);
    console.log(y.stats[0].stat.name + " : " + y.stats[0].base_stat);
    let types = y.types;
    types.forEach(element => {
        console.log(element.type.name);
    });
})*/
